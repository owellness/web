// Barrel for the OWTI domain. App/presentation code imports from
// "@/application/owti" rather than reaching into individual files.
// The domain itself lives in @owellness/shared so web and mobile
// consume a single implementation.
export * from "@owellness/shared/owti";
